export class Tooltip {

    tooltip?: T_View_Thumbler_or_Tooltip;

    // draw_tooltip(is_draw: boolean, is_single?: boolean, orientation?: T_Slider_Orientation) {
    //     let css_class: string = 'SRS__tooltip SRS__tooltip_'
    //     this.tooltip = is_draw
    //         ? is_single
    //             ? orientation === 'horizontal'
    //                 ? document.createElement('div').setAttribute('class', (css_class + 'horizontal'))
    //                 : document.createElement('div').setAttribute('class', (css_class + 'vertical'))
    //             : orientation === 'horizontal'
    //                 ? 
    // }

}